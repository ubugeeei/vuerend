# vapor-islands

Minimal Vue 3.6 Vapor island fixture for checking SSR output, client hydration, and bundle shape.

Run it with:

```sh
pnpm --dir examples/vapor-islands exec vite build
pnpm --dir examples/vapor-islands exec vite --host 127.0.0.1
```
